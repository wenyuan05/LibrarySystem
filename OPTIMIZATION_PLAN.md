## 图书馆管理系统优化方案

### 1. 安全与权限控制

- **密码加密存储（高优先级）**
  - 在 `backend` 中引入 `bcrypt` 或 `argon2` 等密码哈希库。
  - 新增用户（注册/管理员添加）时，对明文密码进行哈希后再写入数据库。
  - 登录接口中，改为查询用户名后使用哈希对比，而不是直接用明文密码匹配。

- **会话与鉴权机制（高优先级）**
  - 使用 JWT 或基于 session 的方式管理登录状态：
    - 登录成功后生成包含用户 `id` 与 `role` 的 token 返回前端。
    - 前端在 `authAPI.login` 中存储 token，并在 `request` 封装中自动通过 `Authorization` header 携带。
  - 所有需要登录访问的接口（如 `/api/books`, `/api/users`, `/api/borrow`, `/api/return` 等）增加鉴权中间件：
    - 验证 token 是否有效；
    - 从 token 中解析用户角色，进行后续权限判断。
  - 针对管理操作（如用户管理、删除书籍等）增加角色控制中间件，仅允许 `admin` 访问。

- **输入校验与基本安全**
  - 对用户名、密码、邮箱、ISBN 等字段做长度和格式校验（可使用 `Joi`、`zod` 或手写校验）。
  - 为 API 统一返回规范化错误结构（如 `{ code, message, details }`），隐藏内部实现细节。

### 2. 数据层与一致性

- **数据库约束完善**
  - 为 `books.isbn` 字段添加 `UNIQUE` 约束，从数据库层彻底保证 ISBN 唯一性。
  - 为 `users.username` 已有的 `UNIQUE` 约束保持不变，并在应用层继续做重复检查，提升用户体验。
  - 根据查询频率，为常用字段（如 `borrow_records.user_id`, `borrow_records.book_id`）添加索引，提高查询性能。

- **借阅流程约束加强**
  - 借阅书籍前除检查书籍 `status` 外，再检查是否存在该书籍尚未归还的借阅记录，防止逻辑缺口。
  - 可考虑在 `borrow_records` 上增加逻辑或约束，避免出现同一本书被多个用户同时标记为“未归还”的情况。

### 3. 前端结构与用户体验

- **引入前端路由**
  - 使用 `react-router` 将当前基于 `activeTab` 的单页切换升级为多路由结构：
    - `"/login"`、`"/books"`、`"/users"`、`"/borrow-records"` 等。
  - 实现“受保护路由”组件：
    - 未登录用户访问受保护页面时自动跳转到 `"/login"`。
    - 普通用户访问仅管理员可用页面（如用户管理）时给出拒绝提示。

- **统一消息提示与状态反馈**
  - 用全局的通知组件（toast/snackbar）替换 `alert`：
    - 成功/失败消息以非阻塞的方式展示。
    - 可在 `request` 封装或自定义 hook 中统一处理 API 错误并弹出提示。

- **列表体验优化**
  - 为 `BookList`、`UserList` 等表格添加：
    - 前端搜索过滤（按标题/作者/用户名/角色等）。
    - 简单分页或“加载更多”以避免一次性渲染大量数据。
  - 后续如数据量增大，可扩展为后端分页接口（`?page=&pageSize=`）。

- **组件复用与 UI 规范化**
  - 抽取常用按钮、表格、对话框等基础组件，减少样式与逻辑重复。
  - 制定统一的颜色、间距、字号等设计规范，集中在样式变量（如 `variables.css`）中管理。

### 4. 工程化与可维护性

- **API 地址与环境配置**
  - 将当前硬编码的 `API_BASE_URL = 'http://localhost:3001/api'` 抽离到环境变量：
    - 在 `.env` 文件中配置 `VITE_API_BASE_URL`。
    - 在 `api.js` 中通过 `import.meta.env.VITE_API_BASE_URL` 获取，便于开发/测试/生产环境切换。

- **统一错误处理**
  - 后端：
    - 增加统一错误处理中间件，捕获路由中的异常并统一格式化输出。
    - 记录关键错误日志（包括栈信息），方便排查。
  - 前端：
    - 在 `request` 函数中规范化错误对象，所有组件只处理统一结构的错误。
    - 可封装自定义 hook（如 `useApiRequest`）以减少重复代码。

- **类型与测试**
  - 前端：
    - 逐步引入 TypeScript，为用户、图书、借阅记录等对象定义类型，提高重构安全性。
  - 后端：
    - 使用 Jest + supertest 为关键 API 编写基础集成测试（登录、增删改查、借阅/归还流程）。

- **日志与监控（可选进阶）**
  - 使用 `morgan` 或自定义中间件记录 HTTP 访问日志。
  - 在生产环境集成简单的健康检查接口和基础监控指标（请求量、错误数等）。

### 5. 推荐实施顺序

1. 实现密码加密、JWT/Session 鉴权与角色中间件，确保基础安全。
2. 完善数据库约束（ISBN 唯一、索引）和借阅流程的严谨性。
3. 升级前端路由体系、统一消息提示和列表体验。
4. 抽离环境配置，统一错误处理，并逐步引入类型和测试。
5. 视项目规模与需求，再考虑日志、监控和更复杂的运维能力。

