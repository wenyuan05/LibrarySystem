# 图书馆管理系统测试用例

## 1. 登录功能测试

### 测试用例 1.1：正常登录（管理员）
- **测试场景**：使用管理员账号登录系统
- **输入数据**：
  - 用户名：admin
  - 密码：admin123
- **预期结果**：
  - 登录成功
  - 跳转到系统主页
  - 显示管理员权限的功能菜单

### 测试用例 1.2：正常登录（普通用户）
- **测试场景**：使用普通用户账号登录系统
- **输入数据**：
  - 用户名：user1
  - 密码：user123
- **预期结果**：
  - 登录成功
  - 跳转到系统主页
  - 显示普通用户权限的功能菜单

### 测试用例 1.3：登录失败（用户名不存在）
- **测试场景**：使用不存在的用户名登录
- **输入数据**：
  - 用户名：nonexistent
  - 密码：123456
- **预期结果**：
  - 登录失败
  - 显示错误信息："Invalid username or password"

### 测试用例 1.4：登录失败（密码错误）
- **测试场景**：使用正确的用户名但错误的密码登录
- **输入数据**：
  - 用户名：admin
  - 密码：wrongpassword
- **预期结果**：
  - 登录失败
  - 显示错误信息："Invalid username or password"

### 测试用例 1.5：登录失败（输入校验）
- **测试场景**：使用不符合规则的用户名和密码登录
- **输入数据**：
  - 用户名：te（少于3个字符）
  - 密码：123（少于6个字符）
- **预期结果**：
  - 前端验证失败
  - 显示错误信息："Username must be between 3 and 20 characters" 和 "Password must be at least 6 characters"

### 测试用例 1.6：多标签页登录隔离
- **测试场景**：同一浏览器打开多个标签页并分别登录不同角色账号
- **操作步骤**：
  1. 在标签页 A 登录普通用户账号
  2. 在标签页 B 登录管理员账号
  3. 回到标签页 A，访问个人资料或借阅记录
  4. 回到标签页 B，访问用户管理或系统设置
- **预期结果**：
  - 标签页 A 仍以普通用户身份发起 API 请求
  - 标签页 B 仍以管理员身份发起 API 请求
  - 任一标签页登录、登出或刷新不会覆盖另一个标签页的登录账号

## 2. 注册功能测试

### 测试用例 2.1：正常注册
- **测试场景**：使用符合规则的信息注册新用户
- **输入数据**：
  - 用户名：newuser
  - 密码：password123
  - 姓名：New User
  - 邮箱：newuser@example.com
- **预期结果**：
  - 注册成功
  - 自动登录系统
  - 跳转到系统主页

### 测试用例 2.2：注册失败（用户名已存在）
- **测试场景**：使用已存在的用户名注册
- **输入数据**：
  - 用户名：admin
  - 密码：password123
  - 姓名：Test User
  - 邮箱：test@example.com
- **预期结果**：
  - 注册失败
  - 显示错误信息："Username already exists"

### 测试用例 2.3：注册失败（输入校验）
- **测试场景**：使用不符合规则的信息注册
- **输入数据**：
  - 用户名：te（少于3个字符）
  - 密码：123（少于6个字符）
  - 姓名：T（少于2个字符）
  - 邮箱：invalid-email（无效邮箱格式）
- **预期结果**：
  - 前端验证失败
  - 显示相应的错误信息

### 测试用例 2.4：找回密码输入校验
- **测试场景**：使用无效邮箱或手机号找回密码
- **操作步骤**：
  1. 打开登录页
  2. 点击 Forgot password?
  3. 不填写邮箱和手机号直接提交
  4. 输入格式错误的邮箱或手机号再次提交
- **预期结果**：
  - 页面显示字段级错误提示
  - 无效输入不会提交到后端
  - 修正输入后错误提示清除

### 测试用例 2.4.1：管理员编辑用户邮箱格式校验
- **测试场景**：管理员编辑用户资料时输入无效邮箱
- **操作步骤**：
  1. 以 admin 登录并打开用户管理页
  2. 编辑一个非 admin 用户
  3. 将邮箱改为 `invalid-email` 并提交
  4. 直接调用 `PUT /api/users/:id`，请求体包含 `{ "email": "invalid-email" }`
- **预期结果**：
  - 前端显示 `Invalid email format`
  - 直接接口请求返回 HTTP 400
  - 后端响应 `Invalid email format`
  - 用户原邮箱不会被修改

### 测试用例 2.5：重置密码输入校验
- **测试场景**：重置密码时输入无效新密码
- **操作步骤**：
  1. 进入 Reset Password 表单
  2. 输入少于 6 位的新密码
  3. 输入与新密码不一致的确认密码
  4. 提交表单
- **预期结果**：
  - 页面显示新密码长度错误和确认密码不一致错误
  - 无效输入不会提交到后端
  - 两次密码一致且满足长度后可继续提交

## 3. 书籍管理功能测试（管理员）

### 测试用例 3.1：添加书籍
- **测试场景**：添加一本新书籍
- **输入数据**：
  - 标题：Test Book
  - 作者：Test Author
  - ISBN：9781234567890
- **预期结果**：
  - 书籍添加成功
  - 显示成功提示
  - 书籍列表中显示新添加的书籍

### 测试用例 3.2：添加书籍失败（ISBN已存在）
- **测试场景**：添加一本ISBN已存在的书籍
- **输入数据**：
  - 标题：Duplicate Book
  - 作者：Test Author
  - ISBN：9781234567890（已存在）
- **预期结果**：
  - 添加失败
  - 显示错误信息："Book with this ISBN already exists"

### 测试用例 3.3：添加书籍失败（输入校验）
- **测试场景**：使用不符合规则的信息添加书籍
- **输入数据**：
  - 标题：（空）
  - 作者：（空）
  - ISBN：123（无效ISBN格式）
- **预期结果**：
  - 前端验证失败
  - 显示相应的错误信息

### 测试用例 3.4：删除书籍
- **测试场景**：删除一本存在的书籍
- **操作步骤**：
  1. 登录管理员账号
  2. 进入书籍管理页面
  3. 选择一本书籍
  4. 点击删除按钮
- **预期结果**：
  - 书籍删除成功
  - 显示成功提示
  - 书籍列表中不再显示该书籍

## 4. 用户管理功能测试（管理员）

### 测试用例 4.1：查看用户列表
- **测试场景**：查看系统中的所有用户
- **操作步骤**：
  1. 登录管理员账号
  2. 进入用户管理页面
- **预期结果**：
  - 显示所有用户的列表
  - 包含用户名、角色、姓名、邮箱等信息

### 测试用例 4.2：添加用户
- **测试场景**：添加一个新用户
- **输入数据**：
  - 用户名：adminuser
  - 密码：password123
  - 角色：admin
  - 姓名：Admin User
  - 邮箱：adminuser@example.com
- **预期结果**：
  - 用户添加成功
  - 显示成功提示
  - 用户列表中显示新添加的用户

### 测试用例 4.3：删除用户
- **测试场景**：删除一个存在的用户
- **操作步骤**：
  1. 登录管理员账号
  2. 进入用户管理页面
  3. 选择一个用户
  4. 点击删除按钮
- **预期结果**：
  - 用户删除成功
  - 显示成功提示
  - 用户列表中不再显示该用户

### 测试用例 4.4：普通用户更新个人资料
- **测试场景**：普通用户编辑自己的姓名、邮箱、电话或地址
- **操作步骤**：
  1. 登录普通用户账号
  2. 进入个人资料页面
  3. 修改姓名、邮箱、电话或地址并提交
- **预期结果**：
  - 个人资料更新成功
  - 请求不会提交角色变更
  - 如果旧前端或缓存请求携带未变化的 `role` 字段，后端不会误判为越权修改角色
  - 非 admin 尝试把 `role` 改成其他值时仍返回 403

## 5. 借阅功能测试

### 测试用例 5.1：借阅书籍
- **测试场景**：用户借阅一本可用的书籍
- **操作步骤**：
  1. 登录用户账号
  2. 进入书籍列表页面
  3. 选择一本状态为"available"的书籍
  4. 点击借阅按钮
- **预期结果**：
  - 借阅成功
  - 显示成功提示
  - 书籍状态变为"borrowed"
  - 借阅记录中显示该记录

### 测试用例 5.1.1：待确认借阅弹窗关闭与取消锁定
- **测试场景**：用户发起借阅后，在确认弹窗中分别关闭弹窗和取消锁定
- **操作步骤**：
  1. 登录普通用户账号
  2. 在书籍列表或书籍详情中借阅一本可用书籍，进入 Confirm Borrowing 弹窗
  3. 观察弹窗中的 `Time left to confirm` 倒计时
  4. 点击 `Not Now` 或右上角关闭按钮
  5. 从书籍列表或借阅记录重新打开同一条待确认借阅的 Confirm Borrowing 弹窗
  6. 点击 `Cancel Lock`
- **预期结果**：
  - `Not Now` 和右上角关闭按钮只关闭弹窗，不取消待确认借阅
  - 重新打开弹窗时倒计时继续基于 `confirm_deadline` 展示剩余时间，不显示为 `00:00`
  - `Cancel Lock` 后待确认记录状态变为 `timeout`
  - 取消锁定后该书不再显示 Confirm Borrow 入口，兼容旧数据中已锁定的副本会恢复可用

### 测试用例 5.2：归还书籍
- **测试场景**：用户归还一本已借阅的书籍
- **操作步骤**：
  1. 登录用户账号
  2. 进入借阅记录页面
  3. 选择一本已借阅但未归还的书籍
  4. 点击归还按钮
- **预期结果**：
  - 归还成功
  - 显示成功提示
  - 书籍状态变为"available"
  - 借阅记录中显示归还日期

### 测试用例 5.2.1：借阅记录跳转图书详情
- **测试场景**：用户或图书管理员从借阅记录打开对应图书详情
- **操作步骤**：
  1. 登录普通用户账号，进入个人借阅记录页面
  2. 点击任意一条借阅记录所在行
  3. 使用管理员或图书管理员账号进入某用户借阅记录页面
  4. 点击任意一条借阅记录所在行
  5. 准备一条关联图书已不存在的借阅记录并点击该记录
- **预期结果**：
  - 有效记录跳转到 `/books/:bookId` 对应图书详情页
  - 从借阅记录跳转到图书详情后，点击 Back 返回原借阅记录页面，而不是固定返回书籍列表
  - 行内 `Return`、`Renew`、`Confirm` 等操作按钮不会触发行跳转
  - 关联图书不存在时显示错误提示，不跳转到空详情页

### 测试用例 5.3：借阅失败（书籍不可用）
- **测试场景**：尝试借阅一本已被借阅的书籍
- **操作步骤**：
  1. 登录用户账号
  2. 进入书籍列表页面
  3. 选择一本状态为"borrowed"的书籍
  4. 点击借阅按钮
- **预期结果**：
  - 借阅失败
  - 显示错误信息："Book is not available"

### 测试用例 5.4：预约记录跳转图书详情
- **测试场景**：用户从预约记录打开对应图书详情
- **操作步骤**：
  1. 登录普通用户账号
  2. 进入 My Reservations 页面
  3. 点击任意一条预约记录所在行
  4. 点击 active 预约记录的 `Cancel` 按钮
  5. 准备一条关联图书已不存在的预约记录并点击该记录
- **预期结果**：
  - 有效预约记录跳转到 `/books/:bookId` 对应图书详情页
  - 从预约记录跳转到图书详情后，点击 Back 返回 My Reservations 页面，而不是固定返回书籍列表
  - `Cancel` 按钮只取消预约，不触发行跳转
  - 关联图书不存在时显示错误提示，不跳转到空详情页

## 6. 权限控制测试

### 测试用例 6.1：普通用户访问管理员功能
- **测试场景**：普通用户尝试访问用户管理页面
- **操作步骤**：
  1. 登录普通用户账号
  2. 尝试访问用户管理页面
- **预期结果**：
  - 访问被拒绝
  - 显示错误信息："Forbidden: insufficient permissions"

### 测试用例 6.2：未登录用户访问受保护页面
- **测试场景**：未登录用户尝试访问系统主页
- **操作步骤**：
  1. 清除浏览器缓存和登录状态
  2. 直接访问系统主页URL
- **预期结果**：
  - 自动跳转到登录页面
  - 显示登录表单

## 7. 数据验证测试

### 测试用例 7.1：ISBN唯一性验证
- **测试场景**：尝试添加两本ISBN相同的书籍
- **操作步骤**：
  1. 登录管理员账号
  2. 添加一本ISBN为9781234567890的书籍
  3. 再次尝试添加ISBN为9781234567890的书籍
- **预期结果**：
  - 第二次添加失败
  - 显示错误信息："Book with this ISBN already exists"

### 测试用例 7.2：用户名唯一性验证
- **测试场景**：尝试注册两个相同用户名的用户
- **操作步骤**：
  1. 注册一个用户名为testuser的用户
  2. 再次尝试注册用户名为testuser的用户
- **预期结果**：
  - 第二次注册失败
  - 显示错误信息："Username already exists"

## 8. Release 2 通知与公告测试

### 测试用例 8.1：预约可借站内通知
- **测试场景**：预约图书在归还审批后恢复可借，预约用户收到通知
- **操作步骤**：
  1. 使用 Reader A 借出一本书并确认借阅
  2. 使用 Reader B 对同一本无可用副本的书创建预约
  3. Reader A 提交归还申请
  4. 管理员或图书管理员进入归还审批页面并审批该记录
  5. Reader B 登录系统
- **预期结果**：
  - 系统在 `notifications` 表生成一条预约可借通知
  - Reader B 侧边栏显示未读通知数量
  - Reader B 进入 `/notifications` 可看到通知内容

### 测试用例 8.1.1：新增可用副本触发预约通知
- **测试场景**：预约图书无可用副本，管理员新增可用副本后预约用户收到通知
- **操作步骤**：
  1. 使用 Reader B 对一本无可用副本的书创建预约
  2. 管理员或图书管理员进入书籍副本管理
  3. 为该书新增一个副本，默认状态为 available
  4. Reader B 查看侧边栏和通知中心
- **预期结果**：
  - 系统在 `notifications` 表生成一条预约可借通知
  - 预约记录 `notification_sent` 更新为 1
  - Reader B 侧边栏未读 badge 立即显示通知数量

### 测试用例 8.1.2：副本状态恢复 available 触发预约通知
- **测试场景**：管理员将副本状态改为 available 后预约用户收到通知
- **操作步骤**：
  1. 使用 Reader B 对一本无可用副本的书创建预约
  2. 管理员或图书管理员将该书某个副本状态从 unavailable/borrowed/reserved 改为 available
  3. Reader B 查看侧边栏和通知中心
- **预期结果**：
  - 系统创建预约可借通知并标记预约已通知
  - Reader B 可在 `/notifications` 看到通知内容

### 测试用例 8.1.3：归还后审批前可支付罚款
- **测试场景**：用户逾期归还后，在图书管理员审批前即可支付罚款
- **操作步骤**：
  1. 准备一条已逾期且状态为 `borrowed` 或 `overdue` 的借阅记录
  2. Reader 提交归还申请
  3. Reader 打开罚款详情或借阅记录中的罚款弹窗
  4. 点击 Pay Fine
  5. 图书管理员再审批该归还申请
- **预期结果**：
  - 提交归还后记录状态为 `returning`，罚款记录显示为 `unpaid`
  - Pay Fine 在审批前成功，罚款记录更新为 `paid`
  - 用户 `total_fine` 同步为 0
  - 管理员审批后记录状态为 `returned`，罚款状态保持 `paid`，不会重复累计罚款

### 测试用例 8.2：通知已读不重复计数
- **测试场景**：用户标记通知已读后，未读数量减少且不再显示为未读
- **操作步骤**：
  1. 使用有未读通知的 Reader 登录
  2. 进入通知页面
  3. 点击单条未读通知或点击 Mark All Read
  4. 切换页面或刷新
- **预期结果**：
  - 被处理的通知 `is_read` 更新为 1
  - 侧边栏未读数量同步减少
  - 已读通知不再以未读状态展示
  - 不需要刷新页面或切换路由即可看到 badge 更新

### 测试用例 8.3：未读公告弹窗提醒
- **测试场景**：用户登录后存在未读已发布公告，系统弹窗提醒
- **操作步骤**：
  1. 管理员进入公告管理页面
  2. 通过 Add Announcement 弹窗创建并发布公告
  3. 使用普通用户登录并进入任意受保护页面
- **预期结果**：
  - 页面显示公告提醒弹窗
  - 弹窗中展示未读公告标题、内容和创建日期
  - 点击 Got It 后写入公告已读记录

### 测试用例 8.4：已读公告不重复提醒
- **测试场景**：用户确认公告后，再次进入系统不重复弹窗
- **操作步骤**：
  1. 使用普通用户确认公告提醒弹窗
  2. 刷新页面或重新登录
  3. 再次进入受保护页面
- **预期结果**：
  - 已确认公告不会再次触发弹窗
  - `/api/announcements/unread/mine` 不返回已读公告

### 测试用例 8.4.1：公告列表完整信息弹窗
- **测试场景**：用户从公告列表查看完整公告内容
- **操作步骤**：
  1. 登录普通用户账号
  2. 进入 Announcements 页面
  3. 点击一条公告卡片
  4. 准备包含长文本或连续长字符串的公告并再次打开
- **预期结果**：
  - 点击公告后弹出公告详情弹窗
  - 弹窗不被公告列表容器裁剪
  - 公告正文完整展示，长文本自动换行，弹窗随内容动态撑开
  - 点击 Close 或遮罩可关闭弹窗

### 测试用例 8.5：公告管理弹窗表单
- **测试场景**：管理员通过弹窗新增和编辑公告
- **操作步骤**：
  1. 管理员进入 `/announcement-management`
  2. 点击 Add Announcement
  3. 填写标题和内容，切换 Publish Announcement 开关
  4. 保存公告
  5. 点击列表中的 Edit 修改公告
- **预期结果**：
  - 新增/编辑表单以弹窗形式显示，不被页面内容层裁切
  - 保存后弹窗关闭并刷新列表
  - 列表展示标题、内容预览、发布状态、创建时间和操作按钮

### 测试用例 8.6：批量 ISBN 导入失败项展示
- **测试场景**：批量导入混合有效、重复、无效和查不到元数据的 ISBN
- **操作步骤**：
  1. 管理员或图书管理员进入书籍管理页面
  2. 打开 Add New Book 的 Batch Import
  3. 输入有效 ISBN、重复 ISBN、格式错误 ISBN 和当前 ISBN provider 查不到的 ISBN
  4. 执行导入
- **预期结果**：
  - 有效书籍导入成功并生成副本
  - 失败项在导入结果中逐条展示 ISBN 和失败原因
  - 重复或无效 ISBN 不会被静默忽略

### 测试用例 8.6.1：ISBN 查询 API 节点选择与测试
- **测试场景**：管理员或图书管理员选择并测试 ISBN 查询节点
- **操作步骤**：
  1. 管理员或图书管理员进入书籍管理页面
  2. 打开 Add New Book
  3. 在 ISBN Lookup API 下拉框中切换 OpenLibrary、Google Books 和 ShowAPI ISBN
  4. 点击 Test Node
  5. 在 Single Book 输入 ISBN 并点击 Search ISBN
  6. 切换到 Batch Import，输入 ISBN 列表并观察预览
- **预期结果**：
  - 节点测试显示 provider 名称、endpoint、可用/不可用、延迟、最后测试时间和失败原因
  - ShowAPI ISBN 未配置 `SHOWAPI_ISBN_APP_KEY` 时显示 key required，测试结果为不可用且提示未配置
  - 单本 ISBN 查询使用当前选定节点
  - ShowAPI 返回的 `gist` 在单本添加和批量导入中保存为书籍 `description`
  - 批量导入预览和最终导入使用当前选定节点
  - 如果当前节点测试为不可用，查询或导入入口被禁用或提示切换节点
  - 当本机 `127.0.0.1:7890` 代理开启时，后端通过 `undici.ProxyAgent` 发起外部 ISBN API 请求；关闭代理后请求回退默认网络
  - 后端安装依赖后启动不应出现 `Cannot find module 'undici'`

### 测试用例 8.7：添加书籍弹窗层级
- **测试场景**：管理员在书籍管理页打开 Add New Book
- **操作步骤**：
  1. 登录管理员账号
  2. 打开 `/book-management`
  3. 点击 Add New Book
- **预期结果**：
  - Add New Book 弹窗显示在书籍页面容器之上
  - 弹窗不被页面卡片、滚动容器或背景内容裁切
  - 点击遮罩或关闭按钮可正常关闭弹窗

### 测试用例 8.8：列表排序按钮文案
- **测试场景**：检查列表排序切换按钮显示
- **操作步骤**：
  1. 打开借阅记录、罚款详情、预约列表和日志页面
  2. 查看排序按钮
  3. 点击排序按钮切换顺序
- **预期结果**：
  - 排序按钮显示 `Ascending` 或 `Descending`
  - 不再显示 `Oldest First` 或 `Newest First`
  - 点击后列表顺序正常切换

### 测试用例 8.9：系统设置分组与可用项
- **测试场景**：管理员查看和编辑系统设置
- **操作步骤**：
  1. 登录管理员账号
  2. 打开 `/system-settings`
  3. 查看设置分组和字段
  4. 开启 Editable mode，修改一个已实现设置并保存
- **预期结果**：
  - 页面以 Borrow Rules 和 Fine Rules 分组卡片展示
  - 仅显示 `borrow_enabled`、`reservation_enabled`、`borrow_period_days`、`max_borrows`、`borrow_confirm_minutes`、`max_renew_times`、`renew_days`、`fine_enabled`、`fine_per_day`、`max_fine` 对应设置
  - 不显示未接入业务逻辑的 System Name、System Version、Max Reservations、Blacklist Days、Late Return Policy、Lost Book Compensation
  - 修改后出现 pending save bar，点击 Save Changes 后保存成功并显示成功提示

### 测试用例 8.10：QQ 邮箱邮件功能
- **测试场景**：验证注册、密码重置、通知和测试邮件的发信触发
- **前置条件**：
  - 本地演示可配置 `EMAIL_ENABLED=true`、`EMAIL_MODE=log`
  - 真实 QQ 邮箱发信需配置 `EMAIL_MODE=smtp`、`SMTP_USER` 和 QQ 邮箱 SMTP 授权码 `SMTP_PASS`
- **操作步骤**：
  1. 启动后端并查看启动日志中的 Email configuration
  2. 管理员打开 `/system-settings`，查看右侧 Email Test 卡片
  3. 在 Email Test 中输入测试收件邮箱并点击 Send Test Email
  4. 打开注册页，填写邮箱后点击 Send Code
  5. 输入邮箱收到的 6 位验证码并注册一个新账号
  6. 发起密码重置请求
  7. 点击密码重置邮件中的 `/login?token=...` 链接
  8. 输入重置邮件中的 6 位验证码并提交新密码
  9. 触发预约到书通知
- **预期结果**：
  - 配置状态接口不返回 SMTP 授权码明文，只返回 `hasPass`
  - 前端 Email Test 卡片显示邮件模式、启用状态和 SMTP 授权是否就绪
  - log 模式下控制台输出邮件摘要，`email_logs` 写入 `logged`
  - smtp 模式且 QQ 邮箱配置正确时可以收到测试、注册验证码、注册确认、密码重置链接、密码重置验证码和通知邮件
  - 注册和重置密码缺少验证码、验证码错误、验证码过期或重复使用时提交失败
  - 密码重置链接进入现有登录页重置密码表单，输入正确验证码后能提交新密码
  - 配置缺失或发信失败时接口/日志返回明确错误，业务主流程不因通知邮件失败而中断

### 测试用例 8.11：Books 页面分页
- **测试场景**：读者浏览 Books 页面大量书籍
- **操作步骤**：
  1. 登录普通用户账号
  2. 打开 `/books`
  3. 查看列表底部分页控件
  4. 点击 Next、Previous、First、Last
  5. 切换搜索、分类和 Available/Borrowed/Reserved 快速筛选
- **预期结果**：
  - 每页最多显示 12 本书
  - 分页摘要显示当前范围和筛选后的总数
  - 首页禁用 First/Previous，末页禁用 Next/Last
  - 搜索、分类或快速筛选变化后自动回到第一页

### 测试用例 8.12：分类管理双栏与分页
- **测试场景**：图书管理员管理大量分类
- **操作步骤**：
  1. 登录图书管理员账号
  2. 打开 `/category-management`
  3. 创建多个分类，使分类数量超过 8 个
  4. 查看 Category List 区域
  5. 鼠标悬停长分类名
  6. 在左侧 Search Categories 中输入关键词并点击放大镜按钮
  7. 点击 First、Previous、Next、Last 分页按钮
- **预期结果**：
  - Create New Category 卡片位于页面左侧
  - Category List 以双栏显示分类条目
  - 长分类名限制在名称区域内最多两行换行显示，不会把 Edit/Delete 按钮挤出卡片
  - 鼠标悬停分类名可查看完整分类名
  - 点击搜索按钮后右侧只展示匹配分类，并自动回到第一页
  - 编辑分类时 Save 和 Cancel 按钮完整显示
  - 每页最多显示 8 个分类
  - 分页摘要显示当前范围和分类总数
  - 首页禁用 First/Previous，末页禁用 Next/Last

### 测试用例 8.13：Release 3 文件清理
- **测试场景**：检查 release 分支项目文件结构
- **操作步骤**：
  1. 查看项目根目录
  2. 查看 `backend` 目录
  3. 执行前端构建
- **预期结果**：
  - 根目录不再包含旧优化计划、重构计划、Release 2 备注、旧 release plan v2、示例 JSON 和 Vite 日志
  - `backend` 目录不再包含一次性数据库检查、迁移、清理和修复脚本
  - 当前运行时代码、环境变量示例、Release 3 文档和构建流程保持可用

### 测试用例 8.14：支付订单终态停止轮询
- **测试场景**：Fine Records 和 Payment Result 页面监听支付订单状态
- **操作步骤**：
  1. 创建一个支付宝罚款支付订单
  2. 保持订单 pending，观察页面会定时刷新状态
  3. 将订单变为 paid、expired 或 failed
  4. 继续停留在页面观察网络请求
- **预期结果**：
  - pending 状态下页面继续轮询订单状态
  - paid 状态后 Fine Records 自动刷新罚款记录并停止轮询
  - expired 或 failed 状态后停止轮询
  - `/payment-result` 页面在终态后不再继续发起定时状态请求

### 测试用例 8.15：数据库 schema 单一定义
- **测试场景**：检查数据库初始化文件维护性
- **操作步骤**：
  1. 搜索 `backend/db.js` 中 `payments`、`email_logs`、`email_verification_codes` 的建表语句
  2. 启动后端或加载数据库模块
- **预期结果**：
  - 每张表只保留一个 `CREATE TABLE IF NOT EXISTS` 定义
  - 支付、邮件日志和邮箱验证码相关索引仍会创建
  - 数据库模块可正常加载

### 测试用例 8.16：阻止删除存在活跃借阅状态的用户
- **测试场景**：管理员删除仍有关联业务状态的用户
- **操作步骤**：
  1. 准备一个存在 `borrowing`、`borrowed`、`overdue` 或 `returning` 借阅记录的用户
  2. 在 User Management 中尝试删除该用户
  3. 准备一个存在 `active` 或 `pending` 预约记录的用户并尝试删除
  4. 尝试删除当前登录管理员账号
  5. 尝试删除其他管理员账号
- **预期结果**：
  - 存在活跃借阅记录时删除被拒绝
  - 存在活跃预约记录时删除被拒绝
  - 当前账号和管理员账号不能被删除
  - 前端展示后端返回的错误信息

### 测试用例 8.17：阻止删除存在活跃借阅或预约状态的图书
- **测试场景**：管理员或图书管理员删除仍处于业务使用中的图书
- **操作步骤**：
  1. 选择存在 `borrowing`、`borrowed`、`overdue` 或 `returning` 记录的图书并尝试删除
  2. 选择存在 `borrowing`、`borrowed` 或 `reserved` 状态副本的图书并尝试删除
  3. 选择存在 `active` 或 `pending` 预约记录的图书并尝试删除
- **预期结果**：
  - 上述活跃或占用状态下删除均被拒绝
  - 不会产生孤立图书、副本、分类关联、借阅记录或预约记录
  - 前端展示后端返回的错误信息

### 测试用例 8.18：导出图书与副本组合数据
- **测试场景**：图书管理员导出馆藏数据
- **操作步骤**：
  1. 使用图书管理员账号登录
  2. 准备包含多个副本和分类的图书
  3. 打开 Book Management
  4. 点击 `Export Books & Copies`
  5. 打开下载的 CSV 文件
- **预期结果**：
  - 请求调用 `GET /api/books/export` 并携带当前图书管理员 token
  - 下载文件名以 `books_with_copies_` 开头
  - 每个副本导出为一行，图书字段在每行重复展示
  - 行数据包含图书 ID、标题、作者、ISBN、馆藏数量、可用数量、分类、副本 ID、副本编号、副本状态和副本位置
  - 包含逗号或引号的 CSV 值被正确转义
  - 管理员和图书管理员可导出，普通用户不能访问接口

### 测试用例 8.19：删除单个可用副本
- **测试场景**：管理员或图书管理员删除多余可用副本
- **操作步骤**：
  1. 打开 Book Management
  2. 对至少有两个副本的图书点击 `Manage Copies`
  3. 选择一个 `available` 状态副本
  4. 点击 `Delete` 并确认
- **预期结果**：
  - 副本行从弹窗中移除
  - `books.total_copies` 减少 1
  - `books.available_copies` 重新计算正确
  - 图书至少保留一个副本

### 测试用例 8.20：阻止不安全副本删除
- **测试场景**：管理员或图书管理员尝试删除不可删除副本
- **操作步骤**：
  1. 尝试删除 `borrowed`、`borrowing`、`reserved` 或 `unavailable` 状态副本
  2. 尝试删除某本书唯一剩余副本
  3. 直接调用接口删除仍有关联活跃借阅记录的可用副本
- **预期结果**：
  - UI 禁用明显不安全的删除操作
  - 后端拒绝所有直接调用的不安全删除请求
  - 被拒绝后图书副本统计不变化

### 测试用例 8.21：副本管理弹窗桌面布局
- **测试场景**：检查 Copy Management 操作列可见性
- **操作步骤**：
  1. 在桌面视口打开 Copy Management
  2. 确认表格展示 Barcode、Status、Location 和 Action 列
  3. 检查 `Confirm` 和 `Delete` 按钮无需拖动底部横向滚动条即可看到
  4. 缩小到移动端宽度重新检查
- **预期结果**：
  - 桌面布局直接展示操作按钮
  - 小屏可横向滚动，但控件保持可读可操作

### 测试用例 8.22：日志清理参数校验
- **测试场景**：管理员使用不同天数参数清理日志
- **操作步骤**：
  1. 使用 `days = 7` 调用 `DELETE /api/logs/clear`
  2. 使用 `days = 0` 调用接口
  3. 使用 `days = "0"` 调用接口
  4. 使用 `-1`、`1.5` 或非数字字符串等非法值调用接口
- **预期结果**：
  - 合法天数清理对应日志
  - 数字 `0` 和字符串 `"0"` 清理全部日志
  - 非法值返回 HTTP 400，且不会删除日志

### 测试用例 8.23：Books 页面 Reserved 快捷筛选
- **测试场景**：Reader 筛选自己已预约的图书
- **操作步骤**：
  1. 使用 Reader 账号登录
  2. 从 Books 页面预约一本书，或使用已有 active 预约
  3. 返回 Books 页面
  4. 点击 `Reserved` 快捷筛选
  5. 从书籍卡片取消预约后再次检查筛选结果
- **预期结果**：
  - 展示当前用户存在 `active` 或 `pending` 预约记录的图书
  - 通过预约记录的 `book_id` 匹配图书，而不是依赖图书级 `status` 字段
  - 取消预约后该书无需整页刷新即可从 `Reserved` 筛选结果中消失

### 测试用例 8.24：Books 页面搜索按钮
- **测试场景**：Reader 通过 Books 页面工具栏搜索图书
- **操作步骤**：
  1. 使用 Reader 账号登录
  2. 打开 Books 页面
  3. 在搜索框输入标题、作者或 ISBN
  4. 点击输入框旁边的搜索图标按钮
  5. 清空输入框并再次点击搜索按钮
- **预期结果**：
  - 搜索图标按钮显示在 Books 页面搜索输入框旁边
  - 点击按钮会按当前输入值重新搜索
  - 清空输入框后点击按钮会恢复完整图书列表

### 测试用例 8.25：Books 列表可用状态回退显示
- **测试场景**：Reader 打开 Books 页面时副本详情仍在加载
- **操作步骤**：
  1. 使用 Reader 账号登录
  2. 打开 Books 页面
  3. 在主书籍列表加载后立即观察书籍卡片
  4. 等待副本详情加载完成
- **预期结果**：
  - 副本详情未加载完成时，书籍卡片使用列表响应中的 `available_copies` 展示可用状态
  - 可借图书不会在副本详情加载中临时显示为 `Borrowed`
  - 副本详情加载完成后，卡片状态与实际可用副本数量一致

### 测试用例 8.26：借阅功能开关
- **测试场景**：管理员全局关闭并重新开启读者借阅
- **操作步骤**：
  1. 使用管理员账号打开 `/system-settings`
  2. 进入 Editable mode，关闭 `Borrowing Enabled` 并保存
  3. 使用 Reader 打开 Books 页面或可借图书详情页
  4. 检查 Borrow 和 Confirm Borrow 控件
  5. 在关闭状态下直接调用 `POST /api/borrow/borrow` 和 `POST /api/borrow/confirm-borrow`
  6. 重新开启 `Borrowing Enabled` 并保存
- **预期结果**：
  - `Borrowing Enabled` 以滑动开关和 Enabled/Disabled 文案展示
  - Reader 借阅和确认借阅按钮禁用并显示 `Borrowing Disabled`
  - `GET /api/system/feature-flags` 返回 `borrow_enabled: false`
  - 两个借阅接口均返回 HTTP 403 和对应关闭提示
  - 重新开启后恢复正常借阅控件和 `borrow_enabled: true`

### 测试用例 8.27：预约功能开关
- **测试场景**：管理员全局关闭并重新开启读者预约
- **操作步骤**：
  1. 使用管理员账号打开 `/system-settings`
  2. 进入 Editable mode，关闭 `Reservations Enabled` 并保存
  3. 使用 Reader 打开无可用副本图书的 Books 页面或详情页
  4. 检查 Reserve 控件
  5. 在关闭状态下直接调用 `POST /api/borrow/reserve`
  6. 在关闭状态下取消已有 active 预约
  7. 重新开启 `Reservations Enabled` 并保存
- **预期结果**：
  - `Reservations Enabled` 以滑动开关和 Enabled/Disabled 文案展示
  - Reader 预约按钮禁用并显示 `Reservations Disabled`
  - `GET /api/system/feature-flags` 返回 `reservation_enabled: false`
  - 新预约接口返回 HTTP 403 和对应关闭提示
  - 已有预约仍可取消
  - 重新开启后恢复正常预约控件和 `reservation_enabled: true`

### 测试用例 8.28：罚款累计功能开关
- **测试场景**：管理员关闭逾期罚款累计但不影响已有罚款支付
- **操作步骤**：
  1. 使用管理员账号打开 `/system-settings`
  2. 进入 Editable mode，关闭 `Fines Enabled` 并保存
  3. 创建或选择关闭后变为逾期的借阅记录
  4. 触发逾期检查或加载借阅记录
  5. 归还该逾期图书
  6. 选择关闭前已逾期且存在预计罚款的未归还记录，等待后续刷新
  7. 通过支付宝罚款支付流程支付已有实际未付罚款
  8. 重新开启 `Fines Enabled`
- **预期结果**：
  - `fine_enabled = 0` 时新增逾期记录罚款为 0
  - 已逾期未归还记录保留当前预计罚款，但关闭期间不继续增长
  - 关闭期间归还图书不会在冻结金额之外新增罚款
  - 已有实际未付罚款仍可正常支付
  - 重新开启后按 `fine_per_day` 恢复正常累计，并受 `max_fine` 单条上限限制

### 测试用例 8.29：支付宝后端配置安全摘要
- **测试场景**：后端加载支付宝沙箱配置且不泄露密钥
- **操作步骤**：
  1. 复制 `backend/.env.example` 为 `backend/.env`
  2. 设置 `ALIPAY_ENABLED=true`，并留空一个必需支付宝配置项
  3. 启动后端服务
  4. 补齐所有必需支付宝配置项并重启后端
- **预期结果**：
  - 后端启动日志包含安全的支付宝配置摘要
  - 本地测试配置使用 `http://localhost:3001/api/payments/alipay/notify` 和 `http://localhost:5173/payment-result`
  - 仅在 `ALIPAY_ENABLED=true` 且缺少必需值时提示缺失配置
  - 启动日志不会打印应用私钥或支付宝公钥内容
  - 配置完整后不再显示缺失配置警告

### 测试用例 8.30：支付宝罚款支付模拟接口
- **测试场景**：用户创建本地模拟支付宝罚款订单并完成支付
- **操作步骤**：
  1. 准备一个存在 `fine > 0` 且 `fine_status = unpaid` 记录的用户
  2. 使用该用户调用 `POST /api/payments/fines/alipay`
  3. 确认响应包含 `out_trade_no`、`qr_code`、`payment_url`、`status = pending` 和关联 `borrow_record_ids`
  4. 调用 `GET /api/payments/:id` 确认订单仍为 pending
  5. 调用 `POST /api/payments/alipay/simulate-notify/:out_trade_no`
  6. 刷新用户罚款记录和收入汇总
  7. 创建另一个订单，使用 `POST /api/payments/:id/expire` 过期后再尝试模拟成功
  8. 尝试过期已支付订单
- **预期结果**：
  - 支付单只包含 `returning` / `returned` 的实际未付罚款，不包含未归还逾期预计罚款
  - 创建支付单不会立即标记罚款已支付
  - 模拟 notify 将订单标记为 `paid`
  - 关联罚款记录变为 `fine_status = paid`
  - `users.total_fine` 按剩余未付罚款重新计算
  - 收入汇总包含已支付金额
  - 重复模拟 notify 幂等，不重复增加收入
  - 已过期订单不能模拟成功，已支付订单不能再过期

### 测试用例 8.31：支付宝沙箱收银台链接生成
- **测试场景**：支付宝启用且配置完整时后端生成沙箱收银台链接
- **操作步骤**：
  1. 在 `backend/.env` 配置 `ALIPAY_ENABLED=true`、`ALIPAY_MODE=sandbox`、沙箱 APP_ID、应用私钥、支付宝公钥、notify URL 和 return URL
  2. 重启后端
  3. 创建可支付罚款订单
  4. 打开返回的 `payment_url`
- **预期结果**：
  - `payment_url` 和 `qr_code` 指向支付宝沙箱网关而不是本地 `/payment-result`
  - `payment_url` 包含已签名的 `alipay.trade.page.pay` 请求、系统订单号和金额
  - `qr_code` 优先使用 `alipay.trade.precreate` 返回内容
  - 旧 pending 订单如果仍保存 page-pay 二维码内容，复用前会刷新为 precreate 二维码
  - 支付宝未启用或配置不完整时回退到本地模拟支付链接
  - 单行 key body、PKCS#1 和 PKCS#8 私钥格式都能正常签名

### 测试用例 8.32：支付宝沙箱异步通知验签
- **测试场景**：后端接收支付宝沙箱通知并完成罚款支付
- **操作步骤**：
  1. 完成或模拟一次沙箱支付，使支付宝请求 `POST /api/payments/alipay/notify`
  2. 确认后端收到 `out_trade_no`、`trade_status`、`sign` 等表单字段
  3. 重新查询订单和用户罚款记录
- **预期结果**：
  - 验签通过且状态为 `TRADE_SUCCESS` 或 `TRADE_FINISHED` 时订单变为 `paid`
  - 关联实际未付罚款变为 `fine_status = paid`
  - 签名无效或订单号未知时返回 `fail`，且不修改罚款状态

### 测试用例 8.33：支付宝沙箱主动查询同步状态
- **测试场景**：本地部署没有公网 notify 时通过轮询查询支付宝状态
- **操作步骤**：
  1. 启用并配置支付宝沙箱
  2. 创建可支付罚款订单并打开沙箱收银台链接
  3. 在沙箱收银台完成支付
  4. 刷新 Fine Records 或 `/payment-result`，或等待轮询
- **预期结果**：
  - pending 订单查询时调用 `alipay.trade.query`
  - 支付宝返回成功状态后本地订单变为 `paid`，关联罚款变为已支付
  - 支付宝返回 `TRADE_CLOSED` 时本地订单变为 `expired`
  - 查询超时、失败或金额不匹配时本地订单保持 pending，前端轮询不中断且不返回 500

### 测试用例 8.34：Income Dashboard 收入折线图与日期查询
- **测试场景**：图书管理员查看月度收入趋势并查询日期范围收入
- **操作步骤**：
  1. 使用图书管理员或管理员账号登录
  2. 准备跨月份的已支付支付宝罚款订单
  3. 打开 `/income-dashboard`
  4. 确认默认折线图展示过去一年每月收入
  5. 查询同一天日期范围
  6. 查询 31 天内日期范围
  7. 查询 180 天内日期范围
  8. 查询超过 180 天日期范围
  9. 点击 `Past Year`
  10. 尝试开始日期晚于结束日期
- **预期结果**：
  - 图表只统计已支付支付宝罚款收入
  - 默认图表按月展示过去一年并包含 0 值月份
  - 同日查询返回单日趋势和范围总额
  - 31 天内按日分桶，180 天内按 7 天分桶，更长范围按月分桶
  - `Past Year` 清空范围输入并恢复默认月度趋势
  - 日期顺序无效时前端拦截提交

### 测试用例 8.35：Income Dashboard 支付记录过滤与分页
- **测试场景**：图书管理员分页查看并过滤支付订单
- **操作步骤**：
  1. 使用图书管理员或管理员账号登录
  2. 准备超过 10 条不同用户、状态和创建日期的支付订单
  3. 打开 `/income-dashboard`
  4. 确认支付列表显示第一页和总数
  5. 点击 `Next` 和 `Previous`
  6. 按订单号、用户名、姓名、状态或用户 ID 关键词搜索
  7. 按创建时间起止日期过滤
  8. 组合关键词、状态和日期过滤
  9. 尝试开始日期晚于结束日期
  10. 点击 `Reset`
- **预期结果**：
  - 列表请求携带 `page`、`page_size`、`keyword`、`status`、`date_from`、`date_to`
  - 分页显示当前页、总页数和总记录数
  - 关键词可匹配订单号、用户名、姓名、状态或用户 ID
  - 创建日期过滤包含边界日期
  - 应用过滤后回到第 1 页
  - 日期顺序无效时前端拦截提交
  - Reset 清空过滤条件并回到第 1 页

### 测试用例 8.36：Fine Records 支付宝模拟支付流程
- **测试场景**：用户从 Fine Records 支付罚款
- **操作步骤**：
  1. 使用存在未付罚款的用户登录
  2. 打开 Fine Records 页面
  3. 点击 `Pay with Alipay`
  4. 确认支付面板显示订单号、金额、二维码和支付链接
  5. 确认 `Simulate Payment Success` 只在沙箱或模拟开关开启时显示
  6. 模拟成功前刷新罚款记录
  7. 点击 `Simulate Payment Success`
- **预期结果**：
  - Fine Records 区分 Payable Fine 和 Estimated Fine
  - 创建 pending 支付单不会立即结清罚款
  - 页面展示支付宝支付 UI，不再调用旧的直接结清接口
  - 直接请求旧 `/api/borrow/pay-fine` 返回 404
  - 支付面板展示真实二维码图片和可打开的 `/payment-result` 链接
  - pending 期间每 2-3 秒轮询订单状态
  - 模拟成功后订单和关联罚款变为已支付，未付罚款总额刷新
  - 支付成功后二维码显示 `public/打勾.png` 完成标记
  - 订单过期时页面提示重新创建订单

### 测试用例 8.37：借阅记录罚款弹窗布局
- **测试场景**：用户从 My Borrow Records 打开包含大量记录的罚款弹窗
- **操作步骤**：
  1. 使用存在大量罚款记录的用户登录并打开 My Borrow Records
  2. 点击 `View Fines`
  3. 弹窗打开时调整或滚动页面
- **预期结果**：
  - 弹窗相对浏览器视口居中，不受借阅记录卡片或容器限制
  - 桌面端弹窗突破基础 600px 限制，给罚款表格足够宽度
  - 长书名自然换行，状态和金额列保持单行可读
  - 窄屏时表格在弹窗内部横向滚动，不压缩为难读竖排

### 测试用例 8.38：支付订单管理与收入看板
- **测试场景**：图书管理员管理本地模拟支付宝罚款订单
- **操作步骤**：
  1. 为用户创建一个可支付罚款订单
  2. 在完成或过期前再次创建同一批罚款订单
  3. 使用图书管理员或管理员打开 `/income-dashboard`
  4. 过滤 `Pending` 支付订单
  5. 过期一个 pending 订单
  6. 对另一个订单执行 `Simulate Payment Success` 并刷新 dashboard
  7. 第一个 pending 订单过期后再次创建同一批罚款订单
- **预期结果**：
  - 同一批罚款存在 pending 订单时复用原订单，不重复创建
  - Income Dashboard 展示总收入、今日收入、本月收入、已支付数量、待支付数量和支付行
  - pending 订单可以标记为 expired
  - expired 订单不会标记罚款已支付
  - pending 过期后再次支付会创建新的 pending 订单
  - 支付成功订单计入收入总额

### 测试用例 8.39：借阅记录罚款弹窗跳转支付页
- **测试场景**：用户从 My Borrow Records 罚款弹窗发起支付
- **操作步骤**：
  1. 使用存在未付罚款的用户登录
  2. 打开 My Borrow Records
  3. 点击 `View Fines`
  4. 在罚款弹窗中点击 `Pay with Alipay`
- **预期结果**：
  - 弹窗关闭并跳转到 `/fines/:userId`
  - 前端不再从弹窗直接请求 `/api/borrow/pay-fine`
  - 用户在 Fine Records 支付宝模拟支付面板完成支付
